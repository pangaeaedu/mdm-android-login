<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!--背景色-->
    <item android:id="@android:id/background" android:drawable="@drawable/icon_progress_dr_cmd_toolbar"/>
    <!--一级进度条的颜色，也可以直接替换成图片-->
    <item android:id="@android:id/progress" >
        <clip>
            <nine-patch android:src="@drawable/icon_indeterminate_dr_cmd_toolbar">
            </nine-patch>
        </clip>
    </item>

</layer-list>