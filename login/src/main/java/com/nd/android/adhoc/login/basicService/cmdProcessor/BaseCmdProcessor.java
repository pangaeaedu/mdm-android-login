package com.nd.android.adhoc.login.basicService.cmdProcessor;

import android.content.Context;
import android.support.annotation.NonNull;

import com.nd.android.adhoc.router_api.facade.template.IProvider;

public class BaseCmdProcessor implements IProvider {
    @Override
    public void init(@NonNull Context pContext) {

    }
}
